import React from 'react';

import Modal from '../UI/Modal/Modal';

const Cart = (props) => {
  return (
    <>
      <Modal onClose={props.onClose}>PRODUCTS</Modal>
    </>
  );
};

export default Cart;
